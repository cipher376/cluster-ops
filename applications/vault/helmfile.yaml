repositories:
- name: vault-helm
  url: https://helm.releases.hashicorp.com

releases:
- name: vault
  namespace: gitlab-managed-apps
  chart: vault-helm/vault
  version: 0.20.1
  installed: true
  values:
    - values.yaml
