apiVersion: argoproj.io/v1alpha1
kind: Application
metadata:
  name: infrastructure-bootstrap
  namespace: argocd
spec:
  project: default
  source:
    repoURL: 'https://gitlab.ncs-cloud.lan/ml/cluster-ops.git'
    targetRevision: HEAD
    path: bootstrap/kyverno
    directory: 
      recurse: true
  destination:
    server: 'https://kubernetes.default.svc'
    namespace: default
  syncPolicy:
    automated:
      prune: true
      selfHeal: true