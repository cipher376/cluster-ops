apiVersion: argoproj.io/v1alpha1
kind: Application
metadata:
  name: smoke-test-bootstrap
  namespace: argocd
spec:
  project: default
  source:
    repoURL: 'https://gitlab.ncs-cloud.lan/ml/smoke-test.git'
    targetRevision: HEAD
    path: jobs
    directory: 
      recurse: true
  destination:
    server: 'https://kubernetes.default.svc'
    namespace: default
  syncPolicy:
    automated:
      prune: true
      selfHeal: true