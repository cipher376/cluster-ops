apiVersion: apps/v1
kind: Deployment
metadata:
  name: jupyter-web-app-deployment
  namespace: kubeflow
spec:
  template:
    metadata:
      annotations:
        inject-shared-resource: "enabled"   # Kyverno injects CA and S3 credentials
    spec:
      containers:
      - name: jupyter-web-app