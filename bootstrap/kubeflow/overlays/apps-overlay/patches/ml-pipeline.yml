apiVersion: apps/v1
kind: Deployment
metadata:
  name: ml-pipeline
  namespace: kubeflow
spec:
  template:
    metadata:
      annotations:
        inject-s3: "enabled"   # Kyverno will inject CA and S3 credentials
    spec:
      containers:
      - name: ml-pipeline-api-server   # adjust container name if needed
        