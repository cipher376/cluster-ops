apiVersion: apps/v1
kind: Deployment
metadata:
  name: tensorboard-controller-deployment
  namespace: kubeflow
spec:
  template:
    metadata:
      annotations:
        inject-shared-resource: "enabled"   # Kyverno will inject CA and S3 credentials
    spec:
      containers:
      - name: manager
        